type CSSGridContainer = {
  // To be defined
};

type PlaceholderRectangle = {
  width: number;
  height: number;
};

type Text = {
  content: string;
};

export { CSSGridContainer, PlaceholderRectangle, Text };